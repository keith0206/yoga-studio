app = "soulful"
primary_region = "sin"

[build]

[env]
  DEBUG = "false"
  PORT = "8000"
  # Volume-backed SQLite (created via `fly volumes create`); not Fly Postgres.
  DATABASE_URL = "sqlite:////data/db.sqlite3"

[http_service]
  internal_port = 8000
  force_https = true
  # Keep one machine up: SQLite volume + cold start made wake unreliable.
  auto_stop_machines = "off"
  auto_start_machines = true
  min_machines_running = 1
  processes = ["app"]

  [[http_service.checks]]
    interval = "15s"
    timeout = "5s"
    grace_period = "60s"
    method = "GET"
    path = "/health/"

[[vm]]
  memory = "512mb"
  cpu_kind = "shared"
  cpus = 1

# Persistent SQLite data — create with:
#   fly volumes create yoga_studio_data --region sin --size 1 -a soulful
[[mounts]]
  source = "yoga_studio_data"
  destination = "/data"
